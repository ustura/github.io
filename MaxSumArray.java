
public class MaxSumArray {

 // input −2, 1, −3, 4, −1, 2, 1, −5, 4
 // out : 4, −1, 2, 1,
 

 //Pseudocode
 // iterate over the arr, starting at beg, calculate sum of subarray for the length of 1,2,... n
 //   keeping the max of the sums seen so far,
  // keep the starndx and len of the max sum seen so far.
  // increment beg ndx, init len to 1, repeat.
  
 public int[] maxSumArray(int[] arr) {
 }//while
 }
 
}
